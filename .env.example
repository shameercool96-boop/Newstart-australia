# Supabase is optional for the demo. Without these values the app uses local mock persistence.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used later when replacing the mock local tools endpoint with live Google Maps data.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
